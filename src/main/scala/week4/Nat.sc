import week4._

week4.Zero.toInt

val two = Zero.successor.successor
two.toInt
val two2 = Zero.successor.successor

(Zero + Zero).toInt

val four = two + two2
four.toInt

val eight = four + four
eight.toInt

val a = two - two
a.toInt

val b = eight - two
b.toInt

val c = two - eight
c.toInt

